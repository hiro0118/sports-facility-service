_format_version: "3.0"
_transform: true

services:
  - host: hiro0118.com
    name: api_gateway
    port: 80
    protocol: http
    routes:
      - name: example_route
        paths:
          - /mock
        strip_path: true